name: Telegram Notification

on:
  workflow_dispatch:
    inputs:
      version:
        description: 'Kernel Version'
        required: true
        type: string
      tag:
        description: 'Release Tag'
        required: true
        type: string
      ksun:
        description: 'Install KernelSU-Next ?'
        required: true
        type: string
      ksunvers:
        description: 'KSUN Version (Leave Empty if None)'
        required: false
        type: string
        default: ''
      susfsvers:
        description: 'SUSFS Version (Leave Empty if None)'
        required: false
        type: string
        default: ''
      llvm:
        description: 'Clang Version'
        required: true
        type: string
      telegram_token:
        description: 'Telegram Bot Token'
        required: false
        type: string
        default: ''
      telegram_chat_id:
        description: 'Telegram Chat ID'
        required: false
        type: string
        default: ''

jobs:
  telegram-release:
    runs-on: ubuntu-latest
    
    steps:
      - name: 📥 Download Latest Release Artifacts
        uses: actions/download-artifact@v4
        with:
          path: artifacts
          pattern: BatAxeKernel-*
          merge-multiple: true
          
      - name: 📊 Count Artifacts
        id: count-artifacts
        run: |
          ARTIFACT_COUNT=$(find artifacts -name "*.zip" -type f | wc -l)
          echo "artifact_count=$ARTIFACT_COUNT" >> $GITHUB_OUTPUT
          echo "Found $ARTIFACT_COUNT artifacts"
          
      - name: 📝 Create Telegram Message
        id: create-message
        run: |
          # Get device list
          ARTIFACT_COUNT=${{ steps.count-artifacts.outputs.artifact_count }}
          if [ "$ARTIFACT_COUNT" -eq 0 ]; then
            DEVICES_LIST="❌ No devices built successfully"
          else
            find artifacts -name "*.zip" -type f -exec basename {} .zip \; | sort | sed 's/^/• /' > devices.txt
            DEVICES_LIST=$(cat devices.txt)
          fi
          
          # Create Telegram message
          cat << EOF > telegram_message.md
          🚀 *BatAxeKernel KSUN ${{ github.event.inputs.version }} Released!*

          *📋 Build Information:*
          • *Version:* ${{ github.event.inputs.version }}
          • *Build Date:* $(date +'%Y-%m-%d')
          • *Toolchain:* Clang ${{ github.event.inputs.llvm }}
          • *KernelSU Next:* ${{ github.event.inputs.ksun }}
          • *KernelSU Version:* ${{ github.event.inputs.ksunvers }}
          • *SUFS Version:* ${{ github.event.inputs.susfsvers }}

          *📱 Supported Devices ($ARTIFACT_COUNT devices):*
          $DEVICES_LIST

          *📥 Download:*
          [GitHub Releases](https://github.com/${{ github.repository }}/releases/tag/${{ github.event.inputs.tag }})

          *💬 Support Group:*
          [Join Telegram Discussion](https://t.me/PapaL3xaKernel/2)

          _Note: Flash via custom recovery and backup your current kernel first!_
          EOF
          
          MESSAGE_CONTENT=$(cat telegram_message.md)
          echo "message<<EOF" >> $GITHUB_OUTPUT
          echo "$MESSAGE_CONTENT" >> $GITHUB_OUTPUT
          echo "EOF" >> $GITHUB_OUTPUT

      - name: 📤 Send Telegram Notification
        uses: appleboy/telegram-action@master
        with:
          to: ${{ github.event.inputs.telegram_chat_id != '' && github.event.inputs.telegram_chat_id || secrets.TELEGRAM_CHAT_ID }}
          token: ${{ github.event.inputs.telegram_token != '' && github.event.inputs.telegram_token || secrets.TELEGRAM_BOT_TOKEN }}
          message: ${{ steps.create-message.outputs.message }}
          parse_mode: markdown

      - name: 🧹 Cleanup Downloaded Artifacts
        if: always()
        run: |
          rm -rf artifacts
          echo "Artifacts cleaned up"